# =======================================================
# TikTok Growth Director — 環境変数設定ファイル
# =======================================================

# Google Places API キー
# 取得方法:
# 1. Google Cloud Console (https://console.cloud.google.com/) にアクセス
# 2. 新しいプロジェクトを作成、または既存のプロジェクトを選択
# 3. 「APIs & Services」 > 「Library」から "Places API" を検索して「有効化(Enable)」
# 4. 「APIs & Services」 > 「Credentials(認証情報)」から "APIキー" を作成
# 5. 発行されたキーを下の値に貼り付けてください。
# 
# ※ このファイルを `.env` という名前にリネームして保存してください。
GOOGLE_PLACES_API_KEY=AIzaSyYourGeneratedAPIKeyHere_xxxxxxxx
